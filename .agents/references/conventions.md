# Convenções

- Rotas finas em `src/app/`; UI em `src/screens/<Feature>/`; comportamento em `hooks/`.
- Skills: `$vercel-react-native-skills`, `$react-native-best-practices`, `$expo-router`.
- Preferir `pnpm` (`pnpm-lock.yaml`).
- Não inventar regra: TBD permanece TBD (schema uNmINeD, envelope da bridge, nome do campo origem).
- Wireframe só via Pencil MCP em `docs/design.pen`.
- Validar: `pnpm typecheck`. Smoke no Android: importar export pequeno, Overworld, Atualizar sem duplicar, Substituir com wipe condicional, restart.
